import * as actionType from './actions';

const initialState = {
  username: "",
  password: "",
  loggedIn: false,
  signUP: false,
  lists: null,
  books: null
}

const reducer = (state = initialState, action) => {

};

export default reducer
